<li>
	<h3>{{burger_name}}</h3>

	&nbsp;&nbsp;<button class="change-devour btn btn-default" data-id="{{id}}" data-newdevour="{{devour}}">
		{{#if devour}}DEVOUR!{{else}}EAT ANOTHER{{/if}}
	</button>
</li>